[workspace]
authors = ["pedestrianlove <jsli@linux.com>"]
channels = ["conda-forge"]
name = "llm"
platforms = ["linux-64"]
version = "0.1.0"

[activation.env]
CUDA_HOME = "$CONDA_PREFIX"

[tasks]

[dependencies]
cuda = "12.8.*"
python = "3.14.*"

[pypi-dependencies]
sglang = ">=0.5.2, <0.6"
